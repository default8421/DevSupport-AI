# 有界工具循环 + 分层记忆 实现计划

**Goal:** 让模型在受限工具子集内自主选工具、填参数（而非现在的硬编码顺序执行），并把只存 Redis、6 小时就失忆的会话记忆改造成"MySQL 权威 + Redis 加速 + 跨会话用户画像 + 滚动摘要"的分层记忆。

**Architecture:** 保留既有确定性 LangGraph 骨架与意图路由，只在专家 Agent 内部引入**有界**工具循环（最多 3 轮迭代，每个专家只看到自己那 3–4 个工具）。模型输出与工具执行之间新增确定性入参校验闸门。记忆层拆成 Redis（`session.py`，纯缓存）与 MySQL（`store.py`，权威）两侧，职责不混。

**Tech Stack:** Python 3.11 / FastAPI / LangGraph / SQLAlchemy 2.x async / Redis / DashScope（OpenAI 兼容）/ pytest + pytest-asyncio

设计依据：`docs/specs/2026-09-04-工具循环与记忆-design.md`

## Global Constraints

- **执行环境**：本机系统 python 是 3.9，项目要求 >=3.11，后端命令**必须**走
  `backend/.venv/bin/python`（等价于 `make test` / `make lint`）。用裸 `python`
  会在 `str | None` 这类语法上直接失败。venv 已就绪（便携 CPython 3.11.16）。
- **不新增任何第三方依赖。** 入参校验手写，不引入 `jsonschema`。
- **不引入数据库迁移工具。** 建表走既有 `Base.metadata.create_all`（`scripts/init_db.py`）。
- 高风险工具（`reset_api_key` / `change_plan` / `refund`）对模型**永久不可见**：`tools_for()` 恒定过滤。
- 租户隔离**只**依据 `ToolContext.tenant_id`（服务端可信路径）。**绝不**从记忆或模型输出中读取 tenant/user 身份。
- 任何"由 LLM 或用户输入派生、且会被重新注入 prompt"的内容（用户画像、滚动摘要），落库前必须过 `app.guardrail.desensitize.desensitize_text()`。
- 签名工具**永不接收、永不返回真实密钥**；`&secret=` 一律为字面量 `***`。
- 时间戳窗口 **300 秒**（源自 `data/knowledge/02-鉴权与签名.md`"不超过 5 分钟"）。
- 用户画像硬上限：**最多 5 条事实，每条 value ≤ 60 字符**。
- 代码注释与文档字符串用中文，与现有代码风格一致；`ruff` line-length = 100。
- 每个 Task 结束提交一次。

---

### Task 1: 工具注册中心 —— 入参校验闸门 + 工具子集过滤

**Files:**
- Modify: `backend/app/tools/registry.py`（在 `openai_tools` 后新增函数；改 `execute`）
- Modify: `backend/pyproject.toml`（给 pytest 加 `pythonpath`）
- Create: `backend/tests/__init__.py`（空文件）
- Test: `backend/tests/test_tool_validation.py`

**Interfaces:**
- Consumes: 既有 `ToolSpec`、`REGISTRY`、`execute()`、`_log_call()`
- Produces:
  - `validate_args(spec: ToolSpec, args: dict) -> str | None` —— 返回错误描述，`None` 表示通过
  - `tools_for(names: list[str], include_high_risk: bool = False) -> list[dict]`
  - `execute()` 行为变更：校验失败返回 `{"ok": False, "error": "参数校验失败: ..."}` 并记 `status="rejected"`

- [ ] **Step 1: 给 pytest 配 pythonpath**

`tests/` 目录没有 `__init__.py` 时 pytest 只会把 `tests/` 自身加进 `sys.path`，`import app` 会失败。
在 `backend/pyproject.toml` 的 `[tool.pytest.ini_options]` 段内加一行：

```toml
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
```

再创建空文件 `backend/tests/__init__.py`。

- [ ] **Step 2: 写失败测试**

创建 `backend/tests/test_tool_validation.py`：

```python
"""工具入参校验：模型输出与工具执行之间的确定性闸门。"""

import pytest

from app.tools.registry import ToolSpec, tools_for, validate_args


def _spec(**kw) -> ToolSpec:
    async def _noop(args, ctx):
        return {}

    base = dict(
        name="demo",
        description="演示工具",
        parameters={
            "type": "object",
            "properties": {
                "request_id": {"type": "string"},
                "minutes": {"type": "integer"},
                "params": {"type": "object"},
            },
            "required": ["request_id"],
        },
        func=_noop,
    )
    base.update(kw)
    return ToolSpec(**base)


def test_通过_合法入参():
    assert validate_args(_spec(), {"request_id": "req_1", "minutes": 60}) is None


@pytest.mark.parametrize("args", [{}, {"minutes": 60}, {"request_id": ""}, {"request_id": None}])
def test_拒绝_缺少必需参数(args):
    err = validate_args(_spec(), args)
    assert err is not None and "request_id" in err


def test_拒绝_未声明参数():
    err = validate_args(_spec(), {"request_id": "req_1", "drop_table": "x"})
    assert err is not None and "drop_table" in err


def test_拒绝_类型错误():
    err = validate_args(_spec(), {"request_id": "req_1", "minutes": "六十"})
    assert err is not None and "minutes" in err


def test_拒绝_布尔冒充整数():
    # isinstance(True, int) 为真，必须单独排除，否则模型传 true 会被当 1 放过
    err = validate_args(_spec(), {"request_id": "req_1", "minutes": True})
    assert err is not None and "minutes" in err


def test_接受_整数用于number字段():
    spec = _spec(parameters={
        "type": "object",
        "properties": {"score": {"type": "number"}},
        "required": [],
    })
    assert validate_args(spec, {"score": 1}) is None


def test_无schema时放行():
    assert validate_args(_spec(parameters={}), {"anything": 1}) is None


def test_tools_for_按名字过滤且排除高风险():
    from app.tools import registry

    safe = _spec(name="safe_tool")
    risky = _spec(name="risky_tool", high_risk=True)
    registry.REGISTRY["safe_tool"] = safe
    registry.REGISTRY["risky_tool"] = risky
    try:
        out = tools_for(["safe_tool", "risky_tool", "不存在的工具"])
        names = [t["function"]["name"] for t in out]
        assert names == ["safe_tool"]
        assert out[0]["type"] == "function"
    finally:
        registry.REGISTRY.pop("safe_tool", None)
        registry.REGISTRY.pop("risky_tool", None)
```

- [ ] **Step 3: 运行测试确认失败**

```bash
cd backend && .venv/bin/python -m pytest tests/test_tool_validation.py -v
```

预期：`ImportError: cannot import name 'validate_args'`

- [ ] **Step 4: 实现**

在 `backend/app/tools/registry.py` 中，`openai_tools()` 函数之后插入：

```python
def tools_for(names: list[str], include_high_risk: bool = False) -> list[dict]:
    """按名字挑出 LLM 可见的工具 schema。

    有界工具循环靠它把每个专家的可见范围压到 3-5 个，控制选错工具率。
    高风险工具恒定排除（include_high_risk 仅供内部后台用途）。
    """
    out = []
    for name in names:
        spec = REGISTRY.get(name)
        if spec is None or (spec.high_risk and not include_high_risk):
            continue
        out.append(
            {
                "type": "function",
                "function": {
                    "name": spec.name,
                    "description": spec.description,
                    "parameters": spec.parameters,
                },
            }
        )
    return out


_TYPE_MAP: dict[str, type | tuple[type, ...]] = {
    "string": str,
    "integer": int,
    "number": (int, float),
    "boolean": bool,
    "object": dict,
    "array": list,
}


def validate_args(spec: ToolSpec, args: dict) -> str | None:
    """按 spec.parameters 做最小校验，返回错误描述；None 表示通过。

    这是模型输出与真实执行之间的确定性闸门：模型可以填错参数，
    但填错的参数不允许进入工具函数。故意不引入 jsonschema 依赖。
    """
    schema = spec.parameters or {}
    props = schema.get("properties") or {}
    if not props:
        return None  # 无 schema 声明的工具不校验

    required = schema.get("required") or []
    missing = [k for k in required if k not in args or args[k] in (None, "")]
    if missing:
        return f"缺少必需参数: {', '.join(missing)}"

    unknown = [k for k in args if k not in props]
    if unknown:
        # 不静默丢弃：静默会把模型的参数错误变成无声的错误答案
        return f"不支持的参数: {', '.join(unknown)}"

    for k, v in args.items():
        if v is None:
            continue
        declared = props.get(k, {}).get("type")
        py = _TYPE_MAP.get(declared)
        if py is None:
            continue
        # bool 是 int 的子类，若声明为数字必须显式排除
        if declared in ("integer", "number") and isinstance(v, bool):
            return f"参数 {k} 类型应为 {declared}，实际为 bool"
        if not isinstance(v, py):
            return f"参数 {k} 类型应为 {declared}，实际为 {type(v).__name__}"
    return None
```

再修改 `execute()`，在高风险拦截之后、`start = time.perf_counter()` 之前插入校验：

```python
    # 高风险工具不允许 AI 直接执行（仅内部显式调用且需标记）
    if spec.high_risk and not ctx.is_internal:
        return {"ok": False, "error": f"工具 {name} 为高风险操作，需人工处理，AI 不可直接执行"}

    # 确定性入参校验：模型填错参数不允许进入工具函数
    invalid = validate_args(spec, args)
    if invalid is not None:
        await _log_call(ctx, name, args, {}, "rejected", 0, invalid)
        return {"ok": False, "error": f"参数校验失败: {invalid}"}

    start = time.perf_counter()
```

`ToolCallLog.status` 为 `String(16)`，`"rejected"` 长度 8，可容纳。

- [ ] **Step 5: 运行测试确认通过**

```bash
cd backend && .venv/bin/python -m pytest tests/test_tool_validation.py -v
```

预期：9 passed

- [ ] **Step 6: 确认既有硬编码调用未被新校验误伤**

现存调用点传的参数必须能过校验。逐一核对：

```bash
cd backend && .venv/bin/python - <<'PY'
import ast, pathlib
# 列出所有 execute("name", {...}) 调用，人工核对参数名是否都在对应 schema 的 properties 中
for p in pathlib.Path("app").rglob("*.py"):
    src = p.read_text(encoding="utf-8")
    for node in ast.walk(ast.parse(src)):
        if isinstance(node, ast.Call) and getattr(node.func, "id", "") == "execute":
            if node.args and isinstance(node.args[0], ast.Constant):
                print(f"{p}:{node.lineno} -> {node.args[0].value}")
PY
```

**执行时的实测结果**：`query_*` 系列全部通过。但 `agents/ticket.py` 组装的
`create_ticket` 实参多带了 `user_id` 与 `conversation_id`，这两个键不在 schema 的
`properties` 里，新闸门会判"不支持的参数"，**打断所有建单**——包括
`supervisor.run()` 异常兜底那条最后防线。

修法不是把这两个键补进 schema：`create_ticket` 一旦在工具循环中对模型可见，
schema 里有 `user_id` 就等于允许模型伪造他人身份建单。正确做法是把身份移进
`ToolContext`，与 `tenant_id` 一视同仁：

```python
@dataclass
class ToolContext:
    """工具执行上下文：身份一律由服务端拥有，不接受模型或调用方伪造。"""

    tenant_id: str
    trace_id: str = ""
    is_internal: bool = False
    user_id: str = ""
    conversation_id: str = ""
```

配套改动：`ticket_tools.create_ticket` 改读 `ctx.user_id` / `ctx.conversation_id`；
`agents/ticket.py` 从 args 移除这两个键并删掉同名函数参数；
`supervisor.py` 构造 `ToolContext` 时注入，两处 `create_from_context` 调用去掉这两个 kwarg。

- [ ] **Step 7: 提交**

```bash
git add backend/app/tools/registry.py backend/pyproject.toml backend/tests/
git commit -m "feat(tools): 入参校验闸门 + 工具子集过滤

- validate_args: required/未声明字段/类型 三项最小校验，手写不引依赖
  bool 冒充 integer 单独排除（isinstance(True, int) 为真）
- tools_for: 按名字过滤，高风险工具恒定不对模型可见
- execute 校验失败记 status=rejected，避免模型参数错误变成无声失败
- 补 pytest pythonpath 配置，此前 tests/ 无法 import app"
```

---

### Task 2: 待签字符串校验工具

**Files:**
- Create: `backend/app/tools/signature.py`
- Modify: `backend/app/tools/registry.py:113-115`（`load_tools()` 增加导入）
- Test: `backend/tests/test_signature.py`

**Interfaces:**
- Consumes: `ToolSpec`、`register`、`ToolContext`（Task 1 的 `validate_args` 自动生效）
- Produces:
  - `build_canonical(params: dict, timestamp: str, api_key: str | None) -> tuple[str, list[str]]`
  - `check_timestamp(ts: str, now: int | None = None) -> list[dict]`
  - `check_encoding(params: dict) -> list[dict]`
  - `strip_secrets(params: dict) -> tuple[dict, list[dict]]`
  - `diff_canonical(expected_keys: list[str], client_string: str) -> list[dict]`
  - 注册工具名 `check_signature_canonical`
  - issue 结构统一为 `{"code": str, "detail": str, "fix": str}`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_signature.py`：

```python
"""待签字符串校验：不接触密钥，覆盖知识库 SIGN_INVALID 四条排查要点中的三条。"""

import time

from app.tools.signature import (
    build_canonical,
    check_encoding,
    check_timestamp,
    diff_canonical,
    strip_secrets,
)


def _codes(issues):
    return [i["code"] for i in issues]


# ---------- 待签字符串构造（知识库 02 节步骤 1-4）----------

def test_按字典序拼接并追加secret占位符():
    canonical, keys = build_canonical(
        {"name": "张三", "idcard": "110101199001011234"},
        timestamp="1718000000",
        api_key="ak_live_xxx",
    )
    assert keys == ["api_key", "idcard", "name", "timestamp"]
    assert canonical == (
        "api_key=ak_live_xxx&idcard=110101199001011234&name=张三&timestamp=1718000000&secret=***"
    )


def test_secret永不出现真实值():
    canonical, _ = build_canonical({"a": "1"}, timestamp="1718000000", api_key=None)
    assert canonical.endswith("&secret=***")
    assert "secret=***" in canonical


def test_api_key缺省时不进入待签串():
    _, keys = build_canonical({"a": "1"}, timestamp="1718000000", api_key=None)
    assert keys == ["a", "timestamp"]


# ---------- 误贴密钥 ----------

def test_剔除误贴的密钥并告警():
    cleaned, issues = strip_secrets(
        {"name": "张三", "secret": "sk_real", "sign": "abc", "signature": "d", "secret_key": "e"}
    )
    assert cleaned == {"name": "张三"}
    assert "SECRET_LEAKED_IN_PARAMS" in _codes(issues)


def test_未贴密钥时无告警():
    cleaned, issues = strip_secrets({"name": "张三"})
    assert cleaned == {"name": "张三"} and issues == []


# ---------- 时间戳（文档：秒级、偏差 ≤ 5 分钟）----------

def test_毫秒级时间戳被指出():
    assert "TIMESTAMP_UNIT" in _codes(check_timestamp("1718000000123", now=1718000000))


def test_非数字时间戳被指出():
    assert "TIMESTAMP_NOT_NUMERIC" in _codes(check_timestamp("2024-06-10", now=1718000000))


def test_超出5分钟窗口被指出():
    issues = check_timestamp(str(1718000000 - 301), now=1718000000)
    assert "TIMESTAMP_SKEW" in _codes(issues)


def test_窗口内不报错():
    assert check_timestamp(str(1718000000 - 299), now=1718000000) == []


def test_默认用当前时间():
    assert check_timestamp(str(int(time.time()))) == []


# ---------- URL 编码 ----------

def test_检测出url编码的值():
    issues = check_encoding({"name": "%E5%BC%A0%E4%B8%89"})
    assert "VALUE_URL_ENCODED" in _codes(issues)
    assert "name" in issues[0]["detail"]


def test_正常值不报编码问题():
    assert check_encoding({"name": "张三", "rate": "99%"}) == []


# ---------- 与用户自拼串 diff ----------

def test_顺序错误定位到第几个参数():
    issues = diff_canonical(
        ["api_key", "idcard", "name", "timestamp"],
        "api_key=x&name=张三&idcard=110&timestamp=1718000000&secret=***",
    )
    order = [i for i in issues if i["code"] == "ORDER_MISMATCH"]
    assert order and "第 2 个" in order[0]["detail"]


def test_缺少参数被指出():
    issues = diff_canonical(
        ["api_key", "idcard", "timestamp"], "api_key=x&timestamp=1718000000&secret=***"
    )
    missing = [i for i in issues if i["code"] == "PARAM_MISSING"]
    assert missing and "idcard" in missing[0]["detail"]


def test_多余参数被指出():
    issues = diff_canonical(
        ["api_key", "timestamp"], "api_key=x&debug=1&timestamp=1718000000&secret=***"
    )
    extra = [i for i in issues if i["code"] == "PARAM_EXTRA"]
    assert extra and "debug" in extra[0]["detail"]


def test_漏掉secret后缀被指出():
    issues = diff_canonical(["api_key", "timestamp"], "api_key=x&timestamp=1718000000")
    assert "SECRET_SUFFIX_MISSING" in _codes(issues)


def test_完全正确时无问题():
    assert diff_canonical(
        ["api_key", "timestamp"], "api_key=x&timestamp=1718000000&secret=***"
    ) == []
```

- [ ] **Step 2: 运行确认失败**

```bash
cd backend && .venv/bin/python -m pytest tests/test_signature.py -v
```

预期：`ModuleNotFoundError: No module named 'app.tools.signature'`

- [ ] **Step 3: 实现**

创建 `backend/app/tools/signature.py`：

```python
"""待签字符串校验工具：不接触 Secret Key，校验签名的"构造过程"。

为什么不算 HMAC：api_key 表只存脱敏值（models.py:66），无密钥字段；让用户把
Secret Key 贴进对话违反知识库安全建议，且脱敏层会将其打码。另外知识库里的
hmac_sha256() 伪代码只有一个入参，规格残缺，照它实现必与真实平台不一致。

本工具覆盖知识库 SIGN_INVALID 四条排查要点中的三条（排序 / 时间戳 / 编码），
第二条（Secret 与 Key 配对）需要密钥，只能在输出中提示用户自查。
"""

import re

from app.guardrail import desensitize
from app.tools.registry import ToolContext, ToolSpec, register

SECRET_PLACEHOLDER = "***"
TIMESTAMP_WINDOW_SECONDS = 300  # 知识库 02 节：偏差不超过 5 分钟

# 用户可能误贴进来的敏感键
_SECRET_LIKE = {"secret", "secret_key", "secretkey", "sign", "signature", "x-sign", "xsign"}
_PCT_ESCAPE = re.compile(r"%[0-9A-Fa-f]{2}")


def strip_secrets(params: dict) -> tuple[dict, list[dict]]:
    """剔除误贴的密钥/签名字段并告警。"""
    cleaned, leaked = {}, []
    for k, v in (params or {}).items():
        if str(k).strip().lower() in _SECRET_LIKE:
            leaked.append(str(k))
        else:
            cleaned[k] = v
    issues = []
    if leaked:
        issues.append(
            {
                "code": "SECRET_LEAKED_IN_PARAMS",
                "detail": f"参数里出现了不应参与拼接的字段：{', '.join(leaked)}",
                "fix": "待签字符串只含业务参数与 api_key、timestamp；"
                       "签名本身不参与拼接。若刚才贴出了 Secret Key，请立即在控制台重置",
            }
        )
    return cleaned, issues


def build_canonical(params: dict, timestamp: str, api_key: str | None) -> tuple[str, list[str]]:
    """按知识库步骤 1-4 构造待签字符串；secret 用占位符，绝不接触真实密钥。"""
    merged = {str(k): "" if v is None else str(v) for k, v in (params or {}).items()}
    if api_key:
        merged["api_key"] = str(api_key)
    merged["timestamp"] = str(timestamp)
    keys = sorted(merged)  # 步骤 2：参数名 ASCII 升序
    joined = "&".join(f"{k}={merged[k]}" for k in keys)  # 步骤 3
    return f"{joined}&secret={SECRET_PLACEHOLDER}", keys  # 步骤 4


def check_timestamp(ts: str, now: int | None = None) -> list[dict]:
    """秒级 + 5 分钟窗口判定。"""
    import time as _time

    raw = str(ts).strip()
    if not raw.isdigit():
        return [
            {
                "code": "TIMESTAMP_NOT_NUMERIC",
                "detail": f"时间戳不是纯数字：{raw[:32]}",
                "fix": "X-Timestamp 应为 10 位秒级 Unix 时间戳，不要用日期字符串",
            }
        ]
    if len(raw) == 13:
        return [
            {
                "code": "TIMESTAMP_UNIT",
                "detail": "时间戳为 13 位毫秒级",
                "fix": "改用 10 位秒级（毫秒值除以 1000 取整）",
            }
        ]
    if len(raw) != 10:
        return [
            {
                "code": "TIMESTAMP_LENGTH",
                "detail": f"时间戳长度为 {len(raw)} 位",
                "fix": "X-Timestamp 应为 10 位秒级 Unix 时间戳",
            }
        ]
    current = int(_time.time()) if now is None else int(now)
    skew = abs(current - int(raw))
    if skew > TIMESTAMP_WINDOW_SECONDS:
        return [
            {
                "code": "TIMESTAMP_SKEW",
                "detail": f"与服务器时间相差 {skew} 秒，超出 {TIMESTAMP_WINDOW_SECONDS} 秒窗口",
                "fix": "校对服务器时区与 NTP 同步；签名请在发起请求时即时生成，不要复用旧时间戳",
            }
        ]
    return []


def check_encoding(params: dict) -> list[dict]:
    """检测被额外 URL 编码的 value（知识库排查要点 4）。"""
    hit = [
        str(k)
        for k, v in (params or {}).items()
        if isinstance(v, str) and _PCT_ESCAPE.search(v)
    ]
    if not hit:
        return []
    return [
        {
            "code": "VALUE_URL_ENCODED",
            "detail": f"这些参数的值出现了 %XX 转义：{', '.join(hit)}",
            "fix": "参与签名的 value 用 UTF-8 原文，不要先做 URL 编码；URL 编码只在最终发请求时施加",
        }
    ]


def _parse_pairs(s: str) -> list[tuple[str, str]]:
    out = []
    for seg in str(s).split("&"):
        if not seg:
            continue
        k, _, v = seg.partition("=")
        out.append((k.strip(), v))
    return out


def diff_canonical(expected_keys: list[str], client_string: str) -> list[dict]:
    """把用户自拼的串与正确构造对比，定位到第几个参数出错。"""
    issues: list[dict] = []
    pairs = _parse_pairs(client_string)
    if not pairs:
        return [
            {
                "code": "CLIENT_STRING_UNPARSEABLE",
                "detail": "无法从提供的字符串里解析出 key=value 结构",
                "fix": "确认格式为 key1=value1&key2=value2&...&secret=你的SecretKey",
            }
        ]

    tail_is_secret = pairs[-1][0].lower() == "secret"
    if not tail_is_secret:
        issues.append(
            {
                "code": "SECRET_SUFFIX_MISSING",
                "detail": "末尾没有 &secret=",
                "fix": "按步骤 4，在拼接串末尾追加 &secret=你的SecretKey 之后再做哈希",
            }
        )
    client_keys = [k for k, _ in pairs if k.lower() != "secret"]

    missing = [k for k in expected_keys if k not in client_keys]
    if missing:
        issues.append(
            {
                "code": "PARAM_MISSING",
                "detail": f"待签串里缺少参数：{', '.join(missing)}",
                "fix": "所有业务参数加上 api_key、timestamp 都要参与拼接",
            }
        )
    extra = [k for k in client_keys if k not in expected_keys]
    if extra:
        issues.append(
            {
                "code": "PARAM_EXTRA",
                "detail": f"待签串里多了参数：{', '.join(extra)}",
                "fix": "只拼接实际发送的业务参数，不要加入调试字段或空值参数",
            }
        )

    # 仅比较双方都有的参数的相对顺序，避免缺参/多参重复报错
    common = [k for k in client_keys if k in expected_keys]
    target = [k for k in expected_keys if k in client_keys]
    if common != target:
        for i, (got, want) in enumerate(zip(common, target)):
            if got != want:
                issues.append(
                    {
                        "code": "ORDER_MISMATCH",
                        "detail": f"第 {i + 1} 个参数应为 {want}，实际为 {got}",
                        "fix": "按参数名 ASCII 升序排序后再拼接",
                    }
                )
                break
    return issues


async def check_signature_canonical(args: dict, ctx: ToolContext) -> dict:
    """校验待签字符串的构造过程，不做 HMAC 校验。"""
    raw_params = args.get("params") or {}
    params, issues = strip_secrets(raw_params)
    timestamp = args.get("timestamp", "")
    api_key = args.get("api_key")
    client_string = args.get("client_string")

    canonical, keys = build_canonical(params, timestamp, api_key)
    issues += check_timestamp(timestamp)
    issues += check_encoding(params)

    if client_string:
        issues += diff_canonical(keys, client_string)
    else:
        issues.append(
            {
                "code": "NO_CLIENT_STRING",
                "detail": "用户未提供自己拼出的待签字符串，无法逐字符比对",
                "fix": "把你代码里拼出的待签字符串贴出来，可精确定位到第几个字符开始不一致",
            }
        )

    return {
        # 脱敏后返回：value 可能含身份证等 PII
        "canonical_string": desensitize.desensitize_text(canonical),
        "sorted_keys": keys,
        "issues": issues,
        "secret_checked": False,
        "note": "本工具未校验 HMAC 结果，不能据此断言签名正确；"
                "若构造无误仍失败，请核对 Secret Key 是否与该 API Key 配对、是否误用测试环境密钥",
    }


register(
    ToolSpec(
        name="check_signature_canonical",
        description=(
            "校验 API 请求签名的待签字符串构造是否正确。检查参数字典序、是否缺少或多余参数、"
            "时间戳是否为秒级且在 5 分钟窗口内、value 是否被额外 URL 编码。"
            "不需要也不接受 Secret Key，因此无法判定签名最终值是否正确。"
            "适用于用户报 SIGN_INVALID / 401 签名错误的场景。"
        ),
        parameters={
            "type": "object",
            "properties": {
                "params": {
                    "type": "object",
                    "description": "参与签名的业务参数键值对，不含签名本身",
                },
                "timestamp": {
                    "type": "string",
                    "description": "用户使用的 X-Timestamp 原值，原样传入不要修正",
                },
                "api_key": {
                    "type": "string",
                    "description": "X-Api-Key，可以是脱敏值",
                },
                "client_string": {
                    "type": "string",
                    "description": "用户自己拼出的待签字符串；有则可逐字符比对定位",
                },
            },
            "required": ["params", "timestamp"],
        },
        func=check_signature_canonical,
        category="diagnostic",
    )
)
```

- [ ] **Step 4: 注册进 load_tools**

修改 `backend/app/tools/registry.py` 末尾的 `load_tools()`：

```python
def load_tools() -> None:
    """导入各工具模块以触发注册。"""
    from app.tools import (  # noqa: F401
        apikey,
        billing_tools,
        logs,
        signature,
        ticket_tools,
    )
```

- [ ] **Step 5: 运行测试确认通过**

```bash
cd backend && .venv/bin/python -m pytest tests/test_signature.py -v
```

预期：18 passed

- [ ] **Step 6: 提交**

```bash
git add backend/app/tools/signature.py backend/app/tools/registry.py backend/tests/test_signature.py
git commit -m "feat(tools): 待签字符串校验工具 check_signature_canonical

不接触 Secret Key，覆盖知识库 SIGN_INVALID 四条排查要点中的三条：
参数字典序与缺漏、时间戳秒级与 5 分钟窗口、value 额外 URL 编码。
误贴密钥会被剔除并告警提示重置。返回 secret_checked=false，
禁止据此断言签名正确。"
```

---

### Task 3: 有界工具循环

**Files:**
- Create: `backend/app/agents/tool_loop.py`
- Test: `backend/tests/test_tool_loop.py`

**Interfaces:**
- Consumes: `client.chat`（`llm/client.py:59`，已支持 `tools=` 且返回 `tool_calls`）、`registry.tools_for`、`registry.execute`、`ToolContext`
- Produces:
  - `ToolLoopResult(content: str, invocations: list[dict], tokens: int, degraded: bool)`
  - `async def run(*, system, query, tool_names, ctx, model, history=None, max_iters=3) -> ToolLoopResult`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_tool_loop.py`：

```python
"""有界工具循环：模型选工具+填参数，最多 N 轮，连续全败熔断。"""

import pytest

from app.agents import tool_loop
from app.llm.client import ChatResult
from app.tools.registry import ToolContext

CTX = ToolContext(tenant_id="t_test", trace_id="tr_test")


def _chat_returning(*results):
    """按顺序返回预置 ChatResult，并记录每次收到的 messages。"""
    seen = []

    async def _fake(messages, *, model=None, tools=None, temperature=0.2):
        seen.append({"messages": list(messages), "tools": tools})
        return results[min(len(seen) - 1, len(results) - 1)]

    _fake.seen = seen
    return _fake


@pytest.fixture(autouse=True)
def _no_real_tools(monkeypatch):
    monkeypatch.setattr(tool_loop.registry, "tools_for", lambda names: [
        {"type": "function", "function": {"name": n, "description": "", "parameters": {}}}
        for n in names
    ])


async def test_模型不调工具时直接返回文本(monkeypatch):
    monkeypatch.setattr(
        tool_loop.client, "chat", _chat_returning(ChatResult(content="直接答复", total_tokens=10))
    )
    r = await tool_loop.run(
        system="s", query="q", tool_names=["demo"], ctx=CTX, model="m"
    )
    assert r.content == "直接答复"
    assert r.invocations == [] and r.tokens == 10 and r.degraded is False


async def test_执行工具后带结果二次询问(monkeypatch):
    first = ChatResult(
        content="",
        tool_calls=[{"id": "c1", "name": "demo", "arguments": '{"request_id": "req_1"}'}],
        total_tokens=5,
    )
    second = ChatResult(content="根据日志得出结论", total_tokens=7)
    fake = _chat_returning(first, second)
    monkeypatch.setattr(tool_loop.client, "chat", fake)

    async def _exec(name, args, ctx):
        assert name == "demo" and args == {"request_id": "req_1"}
        return {"ok": True, "data": {"found": True}}

    monkeypatch.setattr(tool_loop.registry, "execute", _exec)

    r = await tool_loop.run(system="s", query="q", tool_names=["demo"], ctx=CTX, model="m")
    assert r.content == "根据日志得出结论"
    assert r.tokens == 12
    assert len(r.invocations) == 1 and r.invocations[0]["ok"] is True
    # 第二次请求必须带上 assistant(tool_calls) 与 role=tool 两条消息
    roles = [m["role"] for m in fake.seen[1]["messages"]]
    assert "tool" in roles and roles.count("assistant") == 1


async def test_并行执行同一轮的多个工具调用(monkeypatch):
    first = ChatResult(
        content="",
        tool_calls=[
            {"id": "c1", "name": "a", "arguments": "{}"},
            {"id": "c2", "name": "b", "arguments": "{}"},
        ],
        total_tokens=5,
    )
    monkeypatch.setattr(
        tool_loop.client, "chat", _chat_returning(first, ChatResult(content="done", total_tokens=1))
    )
    called = []

    async def _exec(name, args, ctx):
        called.append(name)
        return {"ok": True, "data": {}}

    monkeypatch.setattr(tool_loop.registry, "execute", _exec)
    r = await tool_loop.run(system="s", query="q", tool_names=["a", "b"], ctx=CTX, model="m")
    assert sorted(called) == ["a", "b"] and len(r.invocations) == 2


async def test_非法json参数不崩且记为失败(monkeypatch):
    first = ChatResult(
        content="", tool_calls=[{"id": "c1", "name": "demo", "arguments": "{坏的"}], total_tokens=3
    )
    monkeypatch.setattr(
        tool_loop.client, "chat", _chat_returning(first, ChatResult(content="兜底", total_tokens=1))
    )

    async def _exec(name, args, ctx):  # 不应被调用
        raise AssertionError("非法 JSON 不应进入 execute")

    monkeypatch.setattr(tool_loop.registry, "execute", _exec)
    r = await tool_loop.run(system="s", query="q", tool_names=["demo"], ctx=CTX, model="m")
    assert r.invocations[0]["ok"] is False


async def test_连续两轮全败触发熔断(monkeypatch):
    tc = ChatResult(
        content="", tool_calls=[{"id": "c", "name": "demo", "arguments": "{}"}], total_tokens=1
    )
    monkeypatch.setattr(tool_loop.client, "chat", _chat_returning(tc, tc, tc, tc))

    async def _exec(name, args, ctx):
        return {"ok": False, "error": "工具调用超时(>3s)"}

    monkeypatch.setattr(tool_loop.registry, "execute", _exec)
    r = await tool_loop.run(
        system="s", query="q", tool_names=["demo"], ctx=CTX, model="m", max_iters=5
    )
    assert r.degraded is True
    assert len(r.invocations) == 2  # 第 3 轮不再发起


async def test_迭代用尽后强制不带工具收尾(monkeypatch):
    tc = ChatResult(
        content="", tool_calls=[{"id": "c", "name": "demo", "arguments": "{}"}], total_tokens=1
    )
    fake = _chat_returning(tc, tc, tc, ChatResult(content="最终答复", total_tokens=4))
    monkeypatch.setattr(tool_loop.client, "chat", fake)

    async def _exec(name, args, ctx):
        return {"ok": True, "data": {}}

    monkeypatch.setattr(tool_loop.registry, "execute", _exec)
    r = await tool_loop.run(
        system="s", query="q", tool_names=["demo"], ctx=CTX, model="m", max_iters=3
    )
    assert r.content == "最终答复"
    assert fake.seen[-1]["tools"] is None  # 收尾那次不能再带 tools


async def test_历史只带最近4条(monkeypatch):
    fake = _chat_returning(ChatResult(content="ok", total_tokens=1))
    monkeypatch.setattr(tool_loop.client, "chat", fake)
    history = [{"role": "user", "content": f"m{i}"} for i in range(10)]
    await tool_loop.run(
        system="s", query="q", tool_names=["demo"], ctx=CTX, model="m", history=history
    )
    contents = [m["content"] for m in fake.seen[0]["messages"]]
    assert "m5" not in contents and "m6" in contents
```

- [ ] **Step 2: 运行确认失败**

```bash
cd backend && .venv/bin/python -m pytest tests/test_tool_loop.py -v
```

预期：`ModuleNotFoundError: No module named 'app.agents.tool_loop'`

- [ ] **Step 3: 实现**

创建 `backend/app/agents/tool_loop.py`：

```python
"""有界工具循环：模型在受限工具子集内选工具、填参数，迭代次数有上限。

为什么不做全自主 ReAct：把全部工具暴露给模型会 token 膨胀且选错工具率高。
这里每个专家 Agent 只传自己那 3-5 个工具名，模型只负责"选哪个 + 填什么参"，
路径仍由 LangGraph 确定性编排。入参校验在 registry.execute 内部完成。
"""

import asyncio
import json
import time
from dataclasses import dataclass, field

from app.llm import client
from app.tools import registry
from app.tools.registry import ToolContext

HISTORY_TURNS = 4          # 带入循环的历史消息条数
TOOL_RESULT_MAX_CHARS = 4000  # 单条工具结果注回 prompt 的长度上限
FAIL_ROUNDS_TO_BREAK = 2   # 连续几轮全败即熔断


@dataclass
class ToolLoopResult:
    content: str = ""
    invocations: list[dict] = field(default_factory=list)  # 供 trace 展示模型的决策过程
    tokens: int = 0
    degraded: bool = False  # 熔断：建议调用方转人工


async def _invoke(tc: dict, ctx: ToolContext) -> tuple[dict, dict]:
    """执行单次工具调用，返回 (注回 prompt 的载荷, trace 用的调用记录)。"""
    name = tc.get("name", "")
    start = time.perf_counter()
    try:
        args = json.loads(tc.get("arguments") or "{}")
        if not isinstance(args, dict):
            raise ValueError("arguments 不是 JSON 对象")
    except Exception as e:  # noqa: BLE001  模型可能吐出非法 JSON，不能让它崩掉整轮
        err = f"参数不是合法 JSON: {e}"
        return (
            {"ok": False, "error": err},
            {"name": name, "args": {}, "ok": False, "duration_ms": 0, "error": err},
        )

    payload = await registry.execute(name, args, ctx)
    return (
        payload,
        {
            "name": name,
            "args": args,
            "ok": bool(payload.get("ok")),
            "duration_ms": int((time.perf_counter() - start) * 1000),
            "error": payload.get("error"),
        },
    )


async def run(
    *,
    system: str,
    query: str,
    tool_names: list[str],
    ctx: ToolContext,
    model: str,
    history: list[dict] | None = None,
    max_iters: int = 3,
) -> ToolLoopResult:
    """让模型在 tool_names 范围内自主调用工具，最多 max_iters 轮。"""
    tools = registry.tools_for(tool_names)
    messages: list[dict] = [{"role": "system", "content": system}]
    for m in (history or [])[-HISTORY_TURNS:]:
        messages.append({"role": m["role"], "content": m["content"]})
    messages.append({"role": "user", "content": query})

    result = ToolLoopResult()
    fail_rounds = 0

    for _ in range(max_iters):
        r = await client.chat(messages, model=model, tools=tools)
        result.tokens += r.total_tokens

        if not r.tool_calls:
            result.content = (r.content or "").strip()
            return result

        messages.append(
            {
                "role": "assistant",
                "content": r.content or "",
                "tool_calls": [
                    {
                        "id": tc["id"],
                        "type": "function",
                        "function": {"name": tc["name"], "arguments": tc["arguments"]},
                    }
                    for tc in r.tool_calls
                ],
            }
        )

        outcomes = await asyncio.gather(*[_invoke(tc, ctx) for tc in r.tool_calls])
        any_ok = False
        for tc, (payload, inv) in zip(r.tool_calls, outcomes):
            result.invocations.append(inv)
            any_ok = any_ok or inv["ok"]
            messages.append(
                {
                    "role": "tool",
                    "tool_call_id": tc["id"],
                    "content": json.dumps(payload, ensure_ascii=False, default=str)[
                        :TOOL_RESULT_MAX_CHARS
                    ],
                }
            )

        fail_rounds = 0 if any_ok else fail_rounds + 1
        if fail_rounds >= FAIL_ROUNDS_TO_BREAK:
            result.degraded = True
            break

    # 迭代用尽或熔断：再要一次不带 tools 的收尾，保证用户拿到文本而不是空答复
    closing = await client.chat(
        messages + [{"role": "user", "content": "基于以上工具结果给出最终答复，不要再调用工具。"}],
        model=model,
    )
    result.tokens += closing.total_tokens
    result.content = (closing.content or "").strip()
    return result
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd backend && .venv/bin/python -m pytest tests/test_tool_loop.py -v
```

预期：7 passed

- [ ] **Step 5: 提交**

```bash
git add backend/app/agents/tool_loop.py backend/tests/test_tool_loop.py
git commit -m "feat(agents): 有界工具循环

模型在受限工具子集内选工具+填参数，最多 3 轮迭代，同轮多调用并行执行。
连续 2 轮全部失败触发熔断置 degraded，调用方据此转人工。
迭代用尽后强制发一次不带 tools 的收尾请求，避免返回空答复。
模型吐非法 JSON 参数时记为失败而非崩溃。"
```

---

### Task 4: 专家 Agent 接入工具循环

**Files:**
- Modify: `backend/app/agents/api_diagnostic.py:43-126`（`diagnose` 的证据收集段）
- Modify: `backend/app/agents/billing.py:32-55`（`handle` 的证据收集段）
- Test: `backend/tests/test_specialist_wiring.py`

**Interfaces:**
- Consumes: `tool_loop.run` / `ToolLoopResult`（Task 3）、`check_signature_canonical`（Task 2）
- Produces: `DiagResult` / `BillingResult` 既有字段名**一律不变**（下游 `supervisor.summarize_node` 依赖 `.answer` `.card` `.citations` `.evidence` `.need_ticket` `.need_human` `.tokens`）；两者各新增 `invocations: list[dict] = field(default_factory=list)` 与 `degraded: bool = False`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_specialist_wiring.py`：

```python
"""专家 Agent 接入工具循环后，对外结构必须保持不变（supervisor 依赖这些字段）。"""

import pytest

from app.agents import api_diagnostic, billing, doc_rag, tool_loop
from app.agents.tool_loop import ToolLoopResult
from app.llm.client import ChatResult
from app.tools.registry import ToolContext

CTX = ToolContext(tenant_id="t_test", trace_id="tr")


@pytest.fixture
def _stub(monkeypatch):
    """把工具循环、RAG、总结用 LLM 全部打桩，只验证接线与结构。"""
    async def _loop(**kw):
        _loop.kw = kw
        return ToolLoopResult(
            content="证据收集完成：401 且 Key 已过期",
            invocations=[{"name": "query_apikey_status", "args": {}, "ok": True,
                          "duration_ms": 5, "error": None}],
            tokens=11,
        )

    async def _doc(*a, **kw):
        class _D:
            answer = "文档说明"
            citations = [{"index": 1, "doc_title": "鉴权与签名", "section": "401"}]
            tokens = 3
        return _D()

    async def _chat(messages, **kw):
        return ChatResult(
            content='{"conclusion":"Key 已过期","evidence":["401"],"steps":["重新生成"],'
                    '"need_ticket":false}',
            total_tokens=9,
        )

    monkeypatch.setattr(tool_loop, "run", _loop)
    monkeypatch.setattr(api_diagnostic.tool_loop, "run", _loop)
    monkeypatch.setattr(billing.tool_loop, "run", _loop)
    monkeypatch.setattr(doc_rag, "answer", _doc)
    monkeypatch.setattr(api_diagnostic.client, "chat", _chat)
    monkeypatch.setattr(billing.client, "chat", _chat)
    return _loop


async def test_诊断专家只暴露自己的四个工具(_stub):
    await api_diagnostic.diagnose("401 报错", {"error_code": "AUTH_KEY_EXPIRED"}, CTX)
    assert set(_stub.kw["tool_names"]) == {
        "query_call_log",
        "query_apikey_status",
        "query_recent_call_stats",
        "check_signature_canonical",
    }


async def test_诊断专家不暴露高风险工具(_stub):
    await api_diagnostic.diagnose("退款", {}, CTX)
    assert not {"reset_api_key", "change_plan", "refund"} & set(_stub.kw["tool_names"])


async def test_诊断结果结构不变(_stub):
    r = await api_diagnostic.diagnose("401 报错", {}, CTX)
    assert r.card["conclusion"] == "Key 已过期"
    assert r.answer and r.citations and r.tokens > 0
    assert r.invocations and r.invocations[0]["name"] == "query_apikey_status"


async def test_熔断时置need_ticket(monkeypatch, _stub):
    async def _degraded(**kw):
        return ToolLoopResult(content="工具都失败了", invocations=[], tokens=1, degraded=True)

    monkeypatch.setattr(api_diagnostic.tool_loop, "run", _degraded)
    r = await api_diagnostic.diagnose("401 报错", {}, CTX)
    assert r.need_ticket is True


async def test_账单专家只暴露三个计费工具(_stub):
    await billing.handle("账单为什么涨了", {}, CTX)
    assert set(_stub.kw["tool_names"]) == {"query_plan", "query_usage", "query_bill"}


async def test_账单高风险关键词仍转人工(_stub):
    r = await billing.handle("我要退款", {}, CTX)
    assert r.need_human is True
```

- [ ] **Step 2: 运行确认失败**

```bash
cd backend && .venv/bin/python -m pytest tests/test_specialist_wiring.py -v
```

预期：`AttributeError: module 'app.agents.api_diagnostic' has no attribute 'tool_loop'`

- [ ] **Step 3: 改造 api_diagnostic**

在 `backend/app/agents/api_diagnostic.py` 顶部导入区加入：

```python
from app.agents import doc_rag, tool_loop
```

（原来是 `from app.agents import doc_rag`，改成同时导入 `tool_loop`。）

`DiagResult` 新增两个字段（放在 `card` 之后）：

```python
    invocations: list[dict] = field(default_factory=list)
    degraded: bool = False  # 工具熔断，证据不可信
```

把 `diagnose()` 里从 `# 1. 有 request_id → 先查调用日志` 到 `_, doc_answer, citations, doc_tokens = doc_res`
这一整段**替换**为工具循环 + 并行文档：

```python
DIAG_TOOLS = [
    "query_call_log",
    "query_apikey_status",
    "query_recent_call_stats",
    "check_signature_canonical",
]

_EVIDENCE_SYS = (
    "你是 API 平台诊断助手的证据收集环节。根据用户问题调用工具收集真实证据。\n"
    "工具选择原则：\n"
    "- 用户给了 request_id → 先 query_call_log，它会返回 error_code/endpoint/app_id 供后续使用\n"
    "- 怀疑 Key 过期或失效 → query_apikey_status\n"
    "- 涉及 429/限流/QPS 或想看错误率趋势 → query_recent_call_stats\n"
    "- 报 SIGN_INVALID 或 401 签名错误 → check_signature_canonical。"
    "注意它不校验 HMAC，secret_checked=false 时不得断言签名正确\n"
    "证据够了就用一段话总结收集到的关键事实，不要再调用工具。不要编造工具没返回的数据。"
)


async def diagnose(query: str, entities: dict, ctx: ToolContext, *, is_rate_limit: bool = False) -> DiagResult:
    """模型自主收集证据（工具循环）+ 文档支撑，再组装结构化诊断卡片。"""
    error_code = entities.get("error_code")

    hint = f"\n【已识别实体】{json.dumps(entities, ensure_ascii=False)}" if entities else ""
    if is_rate_limit:
        hint += "\n【提示】本问题已判定为限流类，务必查看近期调用统计。"

    async def _evidence():
        return await tool_loop.run(
            system=_EVIDENCE_SYS,
            query=f"{query}{hint}",
            tool_names=DIAG_TOOLS,
            ctx=ctx,
            model=model_for("diagnose"),
        )

    async def _doc():
        # 热路径：已知错误码直查 error_code 表，跳过 RAG 的 embed/rerank/generate
        if error_code:
            fast = await _error_doc_fast(error_code)
            if fast:
                return fast[0], fast[1], 0
        d = await doc_rag.answer(
            f"{error_code} 含义、原因与处理步骤" if error_code else query,
            error_code=error_code or None,
        )
        return d.answer, d.citations, d.tokens

    loop_res, (doc_answer, citations, doc_tokens) = await asyncio.gather(_evidence(), _doc())

    evidence = {
        "tool_findings": loop_res.content,
        "tool_calls": [
            {"name": i["name"], "ok": i["ok"], "args": i["args"]} for i in loop_res.invocations
        ],
    }
```

紧随其后的 LLM 组装段落，把 `user` 的 `【证据】` 部分改为使用新的 `evidence`
（原代码用 `json.dumps(evidence, ...)`，字段变了但写法不变，无需改动），
并在 `need_ticket` 判定处替换旧的 `request_id` 分支：

```python
    parsed = parse_json(gen.content)
    card = normalize_card(parsed)
    need_ticket = bool(parsed.get("need_ticket", False))
    # 工具连续失败（熔断）时证据不可信，直接转人工
    if loop_res.degraded:
        need_ticket = True
    if not card["conclusion"]:
        card["conclusion"] = gen.content.strip()[:200]

    return DiagResult(
        answer=render_card(card),
        evidence=evidence,
        citations=citations,
        error_code=error_code,
        need_ticket=need_ticket,
        tokens=gen.total_tokens + doc_tokens + loop_res.tokens,
        card=card,
        invocations=loop_res.invocations,
        degraded=loop_res.degraded,
    )
```

`_error_doc_fast` 与 `DiagResult` 其余字段保持原样。`execute` 的直接导入若已无引用，
从 import 行移除（保留 `ToolContext`）。

- [ ] **Step 4: 改造 billing**

`backend/app/agents/billing.py` 导入区改为：

```python
import asyncio

from app.agents import doc_rag, tool_loop
```

`BillingResult` 同样新增两个字段（放在 `card` 之后）：

```python
    invocations: list[dict] = field(default_factory=list)
    degraded: bool = False
```

新增常量与替换证据收集段（原 `rp` / `ru` / `rb` 三次顺序 `execute`）：

```python
BILLING_TOOLS = ["query_plan", "query_usage", "query_bill"]

_BILLING_SYS = (
    "你是 API 平台账单助手的数据收集环节。根据用户问题调用工具取真实数据。\n"
    "- 涉及套餐配额/QPS 上限/单价 → query_plan\n"
    "- 涉及调用量 → query_usage（可传 month，如 2026-08）\n"
    "- 涉及账单金额/发票/费用构成 → query_bill\n"
    "账单为什么变化这类问题，需要同时取用量与账单做对比。\n"
    "数据够了就用一段话列出取到的关键数字，不要再调用工具，不要编造数字。"
)
```

在 `handle()` 内，把 `evidence` 收集三段替换为：

```python
    month = entities.get("month")
    hint = f"\n【用户关注月份】{month}" if month else ""

    async def _data():
        return await tool_loop.run(
            system=_BILLING_SYS,
            query=f"{query}{hint}",
            tool_names=BILLING_TOOLS,
            ctx=ctx,
            model=model_for("billing_explain"),
        )

    async def _doc():
        return await doc_rag.answer("套餐计费规则、账单费用构成与超额计费")

    loop_res, doc = await asyncio.gather(_data(), _doc())
    evidence = {
        "tool_findings": loop_res.content,
        "tool_calls": [{"name": i["name"], "ok": i["ok"]} for i in loop_res.invocations],
    }
    citations = doc.citations
```

返回值的 `tokens` 改为 `gen.total_tokens + doc.tokens + loop_res.tokens`；
`need_human` 保持 `high_risk`，但熔断时也转人工，并透传两个新字段：

```python
        need_human=high_risk or loop_res.degraded,
        invocations=loop_res.invocations,
        degraded=loop_res.degraded,
```

- [ ] **Step 5: 让熔断在 trace 上可见**

设计文档 §6 要求 `degraded` 在链路里显式标记，否则熔断率无法统计。
修改 `backend/app/agents/supervisor.py:158-160` 的 `trace.step` 调用，加上降级前缀：

```python
            tok = getattr(result, "tokens", 0)
            hit = [c.get("doc_title") for c in getattr(result, "citations", [])]
            prefix = "[熔断降级] " if getattr(result, "degraded", False) else ""
            trace.step(name, input_summary=query,
                       output_summary=prefix + getattr(result, "answer", "")[:160],
                       duration_ms=t.ms, token_usage=tok, hit_docs=hit)
```

用 `getattr` 带默认值是为了让 `doc_rag` 这类没有 `degraded` 字段的结果也能安全通过。

- [ ] **Step 6: 运行测试确认通过**

```bash
cd backend && .venv/bin/python -m pytest tests/ -v
```

预期：全部通过（Task 1-4 共 40 项）

- [ ] **Step 7: 确认 supervisor 依赖的字段没被破坏**

```bash
cd backend && grep -n "getattr(result" app/agents/supervisor.py
```

确认 `answer` / `citations` / `need_human` / `need_ticket` / `tokens` 仍存在于两个 Result 上；
`supervisor.py:183-188` 读 `outputs["api_diagnostic"].evidence`（现在内容变了但类型仍是 dict，
工单里会记录工具发现，属预期改进）。

- [ ] **Step 8: 提交**

```bash
git add backend/app/agents/api_diagnostic.py backend/app/agents/billing.py backend/app/agents/supervisor.py backend/tests/test_specialist_wiring.py
git commit -m "feat(agents): 诊断/账单专家接入有界工具循环

api_diagnostic 可见 4 个工具（含签名校验），billing 可见 3 个计费工具，
模型自主选工具与填参数，替代原先的硬编码顺序 execute。
证据收集与文档检索仍并行，保留原有延迟特征。
熔断时置 need_ticket / need_human 转人工。对外 Result 字段名不变。"
```

---

### Task 5: 记忆 —— MySQL 回填 + 实体落库

**Files:**
- Create: `backend/app/memory/store.py`
- Modify: `backend/app/memory/session.py`（新增 `replace_history`）
- Modify: `backend/app/agents/supervisor.py:44-48`（`load_context`）与 `:317-319`（写记忆处）
- Test: `backend/tests/test_memory_store.py`

**Interfaces:**
- Consumes: `Message` / `Conversation` ORM、`AsyncSessionLocal`、`session._hist_key`、`HISTORY_MAX`
- Produces:
  - `store.recent_messages(conv_id: str, limit: int = 20) -> list[dict]`（时间正序）
  - `store.save_conversation_state(conv_id: str, *, entities: dict, intent: str | None) -> None`
  - `session.replace_history(conv_id: str, messages: list[dict]) -> None`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_memory_store.py`：

```python
"""记忆回填：Redis 冷启动或 TTL 过期后，必须能从 MySQL 恢复历史。"""

import pytest

from app.memory import session, store


class _FakeRedis:
    """够用的 Redis 替身：list + pipeline。"""

    def __init__(self):
        self.data: dict[str, list[str]] = {}
        self.expires: dict[str, int] = {}

    async def lrange(self, key, start, end):
        items = self.data.get(key, [])
        return items if end == -1 else items[start : end + 1]

    async def rpush(self, key, val):
        self.data.setdefault(key, []).append(val)

    async def delete(self, key):
        self.data.pop(key, None)

    async def expire(self, key, ttl):
        self.expires[key] = ttl

    async def ltrim(self, key, start, end):
        self.data[key] = self.data.get(key, [])[start:] if end == -1 else self.data.get(key, [])

    def pipeline(self):
        return _FakePipe(self)


class _FakePipe:
    def __init__(self, r):
        self.r, self.ops = r, []

    def delete(self, key):
        self.ops.append(("delete", key, None))
        return self

    def rpush(self, key, val):
        self.ops.append(("rpush", key, val))
        return self

    def expire(self, key, ttl):
        self.ops.append(("expire", key, ttl))
        return self

    async def execute(self):
        for op, key, val in self.ops:
            await getattr(self.r, op)(key, val) if val is not None else await getattr(self.r, op)(key)


@pytest.fixture
def fake_redis(monkeypatch):
    r = _FakeRedis()
    monkeypatch.setattr(session, "get_redis", lambda: r)
    return r


async def test_replace_history重建为给定消息(fake_redis):
    await session.replace_history("c1", [
        {"role": "user", "content": "第一句"},
        {"role": "assistant", "content": "第二句"},
    ])
    got = await session.get_history("c1")
    assert [m["content"] for m in got] == ["第一句", "第二句"]


async def test_replace_history先清空再写(fake_redis):
    await session.replace_history("c1", [{"role": "user", "content": "旧"}])
    await session.replace_history("c1", [{"role": "user", "content": "新"}])
    got = await session.get_history("c1")
    assert [m["content"] for m in got] == ["新"]


async def test_replace_history截断到窗口上限(fake_redis):
    msgs = [{"role": "user", "content": f"m{i}"} for i in range(30)]
    await session.replace_history("c1", msgs)
    got = await session.get_history("c1")
    assert len(got) == session.HISTORY_MAX
    assert got[-1]["content"] == "m29"


async def test_空列表不写入(fake_redis):
    await session.replace_history("c1", [])
    assert await session.get_history("c1") == []


async def test_recent_messages为时间正序(monkeypatch):
    """MySQL 侧按 created_at 倒序取再反转，保证返回是正序。"""
    captured = {}

    class _Row:
        def __init__(self, role, content):
            self.role, self.content = role, content

    class _Result:
        def all(self):
            return [_Row("assistant", "第二句"), _Row("user", "第一句")]  # 倒序

    class _Sess:
        async def __aenter__(self):
            return self

        async def __aexit__(self, *a):
            return False

        async def execute(self, stmt):
            captured["stmt"] = str(stmt)
            return _Result()

    monkeypatch.setattr(store, "AsyncSessionLocal", lambda: _Sess())
    got = await store.recent_messages("c1", limit=20)
    assert [m["content"] for m in got] == ["第一句", "第二句"]
    assert "DESC" in captured["stmt"].upper()  # 必须倒序取最近 N 条再反转
```

- [ ] **Step 2: 运行确认失败**

```bash
cd backend && .venv/bin/python -m pytest tests/test_memory_store.py -v
```

预期：`ImportError: cannot import name 'store' from 'app.memory'`

- [ ] **Step 3: 实现 session.replace_history**

在 `backend/app/memory/session.py` 的 `get_history` 之后插入：

```python
async def replace_history(conv_id: str, messages: list[dict]) -> None:
    """用给定消息整体重建历史列表（MySQL 回填与摘要压缩后重置窗口用）。"""
    if not messages:
        return
    r = get_redis()
    key = _hist_key(conv_id)
    pipe = r.pipeline()
    pipe.delete(key)
    for m in messages[-HISTORY_MAX:]:
        pipe.rpush(key, json.dumps({"role": m["role"], "content": m["content"]}, ensure_ascii=False))
    pipe.expire(key, ENTITY_TTL)
    await pipe.execute()
```

- [ ] **Step 4: 实现 store.py**

创建 `backend/app/memory/store.py`：

```python
"""记忆的 MySQL 侧：会话历史回填与实体/意图落库。

session.py 保持纯 Redis（缓存），本模块承担持久化，职责不混。
存在的原因：Redis 历史有 6 小时 TTL，过期或冷启动后 MySQL 里明明有完整对话，
Agent 却会看到空历史。
"""

from sqlalchemy import select, update

from app.db import AsyncSessionLocal
from app.models import Conversation, Message


async def recent_messages(conv_id: str, limit: int = 20) -> list[dict]:
    """取最近 limit 条消息，返回时间正序，用于回填 Redis。

    created_at 为微秒精度（models.DateTime6），同一秒内多条也能稳定排序。
    """
    async with AsyncSessionLocal() as s:
        rows = (
            await s.execute(
                select(Message.role, Message.content)
                .where(Message.conversation_id == conv_id)
                .order_by(Message.created_at.desc(), Message.id.desc())
                .limit(limit)
            )
        ).all()
    return [{"role": r.role, "content": r.content} for r in reversed(rows)]


async def save_conversation_state(
    conv_id: str, *, entities: dict, intent: str | None
) -> None:
    """把已收集实体与最近意图写回 conversation 表。

    collected_entities 与 latest_intent 两列此前建了但从未写入。
    """
    values: dict = {"collected_entities": entities or {}}
    if intent:
        values["latest_intent"] = intent
    async with AsyncSessionLocal() as s:
        await s.execute(update(Conversation).where(Conversation.id == conv_id).values(**values))
        await s.commit()
```

- [ ] **Step 5: 接入 supervisor**

`backend/app/agents/supervisor.py` 导入区加入：

```python
from app.memory import session, store as mem_store
```

（原为 `from app.memory import session`。）

`load_context` 改为：

```python
    async def load_context(state: AgentState) -> dict:
        conv = state["conversation_id"]
        history = await session.get_history(conv)
        if not history:
            # Redis 冷启动或 TTL 过期：从 MySQL 回填，否则明明有历史却失忆
            history = await mem_store.recent_messages(conv, limit=session.HISTORY_MAX)
            if history:
                await session.replace_history(conv, history)
        entities = await session.get_entities(conv)
        return {"history": history, "collected_entities": entities}
```

在 `run()` 里写记忆那段（`await session.append_message(...)` 两行之后）追加落库：

```python
    # 记忆：写入本轮对话
    await session.append_message(conversation_id, "user", query)
    await session.append_message(conversation_id, "assistant", result["answer"])
    # 实体与意图落库（此前 conversation 的两列从未被写入）
    await mem_store.save_conversation_state(
        conversation_id,
        entities=result["entities"],
        intent=result["intent"],
    )
```

- [ ] **Step 6: 运行测试确认通过**

```bash
cd backend && .venv/bin/python -m pytest tests/ -v
```

预期：全部通过（累计 45 项）

- [ ] **Step 7: 提交**

```bash
git add backend/app/memory/ backend/app/agents/supervisor.py backend/tests/test_memory_store.py
git commit -m "fix(memory): Redis 冷启动从 MySQL 回填 + 实体意图落库

此前 load_context 只读 Redis，6 小时 TTL 过期或进程冷启动后
MySQL 里有完整对话但 Agent 看到空历史，多轮指代直接失效。
新增 memory/store.py 承担 MySQL 侧，session.py 保持纯 Redis。
顺带写入 conversation.collected_entities 与 latest_intent
（两列建表就有，此前从未写入）。"
```

---

### Task 6: 记忆 —— 跨会话用户画像

**Files:**
- Modify: `backend/app/models.py`（新增 `UserMemory`；import 加 `UniqueConstraint`）
- Create: `backend/app/memory/user_profile.py`
- Modify: `backend/app/agents/supervisor.py`（`load_context` 注入、`run()` 写入）
- Modify: `backend/app/agents/state.py`（`AgentState` 加 `user_profile: str`）
- Test: `backend/tests/test_user_profile.py`

**Interfaces:**
- Consumes: `desensitize.desensitize_text`、`AsyncSessionLocal`
- Produces:
  - ORM `UserMemory`（表 `user_memory`）
  - `user_profile.derive_facts(entities: dict, plan_name: str | None) -> list[tuple[str, str]]`
  - `user_profile.remember(tenant_id, user_id, entities, plan_name=None) -> None`
  - `user_profile.profile_line(tenant_id, user_id) -> str`（无记录返回 `""`）
  - 常量 `MAX_FACTS = 5`、`MAX_VALUE_LEN = 60`

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_user_profile.py`：

```python
"""跨会话用户画像：结构化派生事实，不存自由文本；严格租户隔离。"""

from app.memory import user_profile as up


def test_派生_端点与错误码与appid():
    facts = up.derive_facts(
        {"endpoint": "/v1/idcard/verify", "error_code": "SIGN_INVALID", "app_id": "app_1"},
        plan_name=None,
    )
    assert ("endpoint", "/v1/idcard/verify") in facts
    assert ("recurring_error", "SIGN_INVALID") in facts
    assert ("app_id", "app_1") in facts


def test_request_id与状态码刻意不入库():
    facts = up.derive_facts(
        {"request_id": "req_abc123", "http_status": "401"}, plan_name=None
    )
    assert facts == []


def test_套餐名来自专家而非实体():
    facts = up.derive_facts({}, plan_name="专业版")
    assert facts == [("plan", "专业版")]


def test_空值与空白被忽略():
    assert up.derive_facts({"endpoint": "", "error_code": "   ", "app_id": None}, None) == []


def test_超长值被截断():
    long_ep = "/v1/" + "a" * 200
    facts = up.derive_facts({"endpoint": long_ep}, None)
    assert len(facts[0][1]) <= up.MAX_VALUE_LEN


def test_派生值会脱敏():
    # 身份证号不该被固化进长期记忆
    facts = up.derive_facts({"endpoint": "/v1/x?idcard=110101199001011234"}, None)
    assert "110101199001011234" not in facts[0][1]


def test_profile_line无记录时为空串(monkeypatch):
    async def _fetch(tenant_id, user_id):
        return []

    monkeypatch.setattr(up, "_fetch_facts", _fetch)
    import asyncio

    assert asyncio.get_event_loop().run_until_complete(up.profile_line("t", "u")) == ""


async def test_profile_line拼接且限制条数(monkeypatch):
    rows = [
        {"kind": "endpoint", "value": "/v1/idcard/verify", "hits": 9},
        {"kind": "recurring_error", "value": "SIGN_INVALID", "hits": 3},
        {"kind": "plan", "value": "专业版", "hits": 1},
        {"kind": "app_id", "value": "app_1", "hits": 1},
        {"kind": "endpoint", "value": "/v1/ocr", "hits": 1},
        {"kind": "endpoint", "value": "/v1/face", "hits": 1},
    ]

    async def _fetch(tenant_id, user_id):
        return rows[: up.MAX_FACTS]

    monkeypatch.setattr(up, "_fetch_facts", _fetch)
    line = await up.profile_line("t", "u")
    assert "该用户历史特征" in line
    assert "反复遇到 SIGN_INVALID（3 次）" in line
    assert "/v1/face" not in line  # 超过 MAX_FACTS 的不进来


async def test_出现一次的错误码不说反复(monkeypatch):
    async def _fetch(tenant_id, user_id):
        return [{"kind": "recurring_error", "value": "TIMEOUT", "hits": 1}]

    monkeypatch.setattr(up, "_fetch_facts", _fetch)
    line = await up.profile_line("t", "u")
    assert "反复" not in line and "TIMEOUT" in line
```

- [ ] **Step 2: 运行确认失败**

```bash
cd backend && .venv/bin/python -m pytest tests/test_user_profile.py -v
```

预期：`ModuleNotFoundError: No module named 'app.memory.user_profile'`

- [ ] **Step 3: 加 ORM 模型**

`backend/app/models.py` 的 sqlalchemy import 列表中加入 `UniqueConstraint`：

```python
from sqlalchemy import (
    JSON,
    BigInteger,
    DateTime,
    ForeignKey,
    Integer,
    String,
    Text,
    UniqueConstraint,
    func,
)
```

在 `Message` 类之后插入：

```python
class UserMemory(Base):
    """用户级长期记忆：跨会话的结构化派生事实。

    只存被识别为实体的结构化值，不存自由文本——用户说什么都不会被原样
    固化再回灌进 prompt，以此限制注入面。所有查询必须带 tenant_id。
    """

    __tablename__ = "user_memory"
    __table_args__ = (
        UniqueConstraint("tenant_id", "user_id", "kind", "value", name="uq_user_memory"),
    )

    id: Mapped[int] = mapped_column(BigInteger, primary_key=True, autoincrement=True)
    tenant_id: Mapped[str] = mapped_column(String(64), index=True)
    user_id: Mapped[str] = mapped_column(String(64), index=True)
    kind: Mapped[str] = mapped_column(String(32))  # endpoint/recurring_error/app_id/plan
    value: Mapped[str] = mapped_column(String(128))
    hits: Mapped[int] = mapped_column(Integer, default=1)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=_now, onupdate=_now)
```

- [ ] **Step 4: 实现 user_profile.py**

创建 `backend/app/memory/user_profile.py`：

```python
"""跨会话用户画像：按 tenant_id + user_id 累积结构化事实。

安全约束（见设计文档 §4.3）：
- 只存结构化派生值，不存自由文本，限制 prompt 注入面
- 入库前脱敏，防止用户贴过的密钥/PII 被长期固化后回显
- 仅作上下文，不得作为任何授权依据；工具鉴权一律走 ToolContext.tenant_id
- 所有读写都带 tenant_id 条件，防跨租户泄漏
"""

from sqlalchemy import select
from sqlalchemy.dialects.mysql import insert as mysql_insert

from app.db import AsyncSessionLocal
from app.guardrail import desensitize
from app.models import UserMemory

MAX_FACTS = 5       # 最多注入 5 条，避免长期记忆无声推高每轮 token
MAX_VALUE_LEN = 60

# 实体键 -> 记忆种类。request_id / http_status 刻意不入库：
# 一次性标识长期留存无复用价值且会无限膨胀。
_ENTITY_TO_KIND = {
    "endpoint": "endpoint",
    "error_code": "recurring_error",
    "app_id": "app_id",
}

_KIND_LABEL = {
    "endpoint": "常用",
    "app_id": "应用",
    "plan": "套餐",
}


def derive_facts(entities: dict, plan_name: str | None) -> list[tuple[str, str]]:
    """从实体与专家结果派生 (kind, value) 列表。"""
    facts: list[tuple[str, str]] = []
    for ent_key, kind in _ENTITY_TO_KIND.items():
        raw = (entities or {}).get(ent_key)
        if not isinstance(raw, str) or not raw.strip():
            continue
        clean = desensitize.desensitize_text(raw.strip())[:MAX_VALUE_LEN]
        facts.append((kind, clean))
    if plan_name and str(plan_name).strip():
        facts.append(("plan", str(plan_name).strip()[:MAX_VALUE_LEN]))
    return facts


async def remember(
    tenant_id: str, user_id: str, entities: dict, plan_name: str | None = None
) -> None:
    """把派生事实 upsert 进长期记忆，重复出现累加 hits。"""
    facts = derive_facts(entities, plan_name)
    if not facts or not tenant_id or not user_id:
        return
    async with AsyncSessionLocal() as s:
        for kind, value in facts:
            stmt = mysql_insert(UserMemory).values(
                tenant_id=tenant_id, user_id=user_id, kind=kind, value=value, hits=1
            )
            # 同一 (租户,用户,种类,值) 再次出现即累加，不新增行
            await s.execute(
                stmt.on_duplicate_key_update(hits=UserMemory.__table__.c.hits + 1)
            )
        await s.commit()


async def _fetch_facts(tenant_id: str, user_id: str) -> list[dict]:
    """取该用户 hits 最高的前 MAX_FACTS 条。查询必须带 tenant_id。"""
    async with AsyncSessionLocal() as s:
        rows = (
            await s.execute(
                select(UserMemory.kind, UserMemory.value, UserMemory.hits)
                .where(UserMemory.tenant_id == tenant_id, UserMemory.user_id == user_id)
                .order_by(UserMemory.hits.desc(), UserMemory.updated_at.desc())
                .limit(MAX_FACTS)
            )
        ).all()
    return [{"kind": r.kind, "value": r.value, "hits": r.hits} for r in rows]


async def profile_line(tenant_id: str, user_id: str) -> str:
    """拼成一行紧凑的 system 提示；无记录返回空串（不注入畸形空提示）。"""
    rows = await _fetch_facts(tenant_id, user_id)
    if not rows:
        return ""
    parts = []
    for r in rows[:MAX_FACTS]:
        if r["kind"] == "recurring_error":
            # hits>=2 才称"反复"，出现一次就说反复会误导诊断
            parts.append(
                f"近期反复遇到 {r['value']}（{r['hits']} 次）"
                if r["hits"] >= 2
                else f"曾遇到 {r['value']}"
            )
        else:
            parts.append(f"{_KIND_LABEL.get(r['kind'], r['kind'])} {r['value']}")
    return "该用户历史特征：" + "；".join(parts)
```

- [ ] **Step 5: 接入 supervisor**

`backend/app/agents/state.py` 的 `AgentState` 在 `collected_entities` 之后加一行：

```python
    user_profile: str                # 跨会话用户画像（仅上下文，不含授权信息）
```

`supervisor.py` 导入区加入 `user_profile`：

```python
from app.memory import session, store as mem_store, user_profile
```

`load_context` 并行取画像：

```python
    async def load_context(state: AgentState) -> dict:
        conv = state["conversation_id"]
        history = await session.get_history(conv)
        if not history:
            history = await mem_store.recent_messages(conv, limit=session.HISTORY_MAX)
            if history:
                await session.replace_history(conv, history)
        entities, profile = await asyncio.gather(
            session.get_entities(conv),
            user_profile.profile_line(state["tenant_id"], state.get("user_id", "")),
        )
        return {"history": history, "collected_entities": entities, "user_profile": profile}
```

把画像注入专家的证据收集环节——`specialists_node` 里给两个专家的 query 前缀加上画像：

```python
        profile = state.get("user_profile") or ""
        profile_hint = f"\n【{profile}】" if profile else ""
```

并把 `profile_hint` 追加到传给 `api_diagnostic.diagnose` 与 `billing.handle` 的 `query`：

```python
        if "api_diagnostic" in route:
            coros.append(_run_agent("api_diagnostic", api_diagnostic.diagnose(
                query + profile_hint, entities, ctx, is_rate_limit=state["intent"] == "rate_limit")))
        if "billing" in route:
            coros.append(_run_agent("billing", billing.handle(query + profile_hint, entities, ctx)))
```

`run()` 末尾在 `save_conversation_state` 之后写入画像：

```python
    # 跨会话画像：仅结构化派生事实，入库前已脱敏
    await user_profile.remember(tenant_id, user_id, result["entities"])
```

- [ ] **Step 6: 建表并跑测试**

```bash
cd backend && .venv/bin/python -m pytest tests/ -v
```

预期：全部通过（累计 54 项）

新表通过既有建表脚本创建（`Base.metadata.create_all` 会带上 `UserMemory`）：

```bash
cd backend && .venv/bin/python -m scripts.init_db
```

- [ ] **Step 7: 提交**

```bash
git add backend/app/models.py backend/app/memory/user_profile.py backend/app/agents/state.py backend/app/agents/supervisor.py backend/tests/test_user_profile.py
git commit -m "feat(memory): 跨会话用户画像 user_memory

按 tenant_id + user_id 累积结构化派生事实（常用端点/反复出现的错误码/
应用/套餐），hits 最高的前 5 条注入专家 Agent 的证据收集环节。
只存被识别为实体的结构化值、不存自由文本，且入库前脱敏，以此限制
prompt 注入面并防止 PII 被长期固化。request_id/http_status 刻意不入库。
所有读写都带 tenant_id 条件。"
```

---

### Task 7: 记忆 —— 滚动摘要压缩

**Files:**
- Modify: `backend/app/memory/session.py`（摘要读写 + `SUMMARY_KEEP`）
- Create: `backend/app/memory/compact.py`
- Modify: `backend/app/agents/supervisor.py`（`load_context` 注入摘要、`run()` 触发压缩）
- Modify: `backend/app/agents/state.py`（加 `history_summary: str`）
- Test: `backend/tests/test_compact.py`

**Interfaces:**
- Consumes: `session.get_history` / `replace_history`、`client.chat`、`settings.llm_model_small`、`desensitize`
- Produces:
  - `session.get_summary(conv_id) -> str`、`session.set_summary(conv_id, text) -> None`
  - `session.SUMMARY_KEEP = 10`
  - `compact.maybe_compact(conv_id: str) -> bool`（是否执行了压缩）

- [ ] **Step 1: 写失败测试**

创建 `backend/tests/test_compact.py`：

```python
"""滚动摘要：历史超窗时把最旧的压成摘要，保留最近若干条原文。"""

from app.llm.client import ChatResult
from app.memory import compact, session
from tests.test_memory_store import _FakeRedis  # 复用 Redis 替身


import pytest


@pytest.fixture
def fake_redis(monkeypatch):
    r = _FakeRedis()
    monkeypatch.setattr(session, "get_redis", lambda: r)
    return r


async def test_未超窗不压缩(fake_redis, monkeypatch):
    await session.replace_history("c1", [{"role": "user", "content": f"m{i}"} for i in range(5)])

    async def _boom(*a, **kw):
        raise AssertionError("未超窗不应调用 LLM")

    monkeypatch.setattr(compact.client, "chat", _boom)
    assert await compact.maybe_compact("c1") is False


async def test_超窗时压缩并保留最近N条(fake_redis, monkeypatch):
    msgs = [{"role": "user", "content": f"m{i}"} for i in range(session.HISTORY_MAX)]
    await session.replace_history("c1", msgs)

    async def _chat(messages, **kw):
        return ChatResult(content="用户先问了签名再问了限流", total_tokens=8)

    monkeypatch.setattr(compact.client, "chat", _chat)
    assert await compact.maybe_compact("c1") is True

    kept = await session.get_history("c1")
    assert len(kept) == session.SUMMARY_KEEP
    assert kept[-1]["content"] == f"m{session.HISTORY_MAX - 1}"
    assert "签名" in await session.get_summary("c1")


async def test_摘要落库前脱敏(fake_redis, monkeypatch):
    msgs = [{"role": "user", "content": f"m{i}"} for i in range(session.HISTORY_MAX)]
    await session.replace_history("c1", msgs)

    async def _chat(messages, **kw):
        # 模型摘要里带出了用户贴过的密钥，绝不能原样固化
        return ChatResult(content="用户的 key 是 ak_demo_abcdef1234567890abcdef12", total_tokens=5)

    monkeypatch.setattr(compact.client, "chat", _chat)
    await compact.maybe_compact("c1")
    assert "ak_demo_abcdef1234567890abcdef12" not in await session.get_summary("c1")


async def test_已有摘要时做增量合并(fake_redis, monkeypatch):
    await session.set_summary("c1", "此前：用户在排查 401")
    msgs = [{"role": "user", "content": f"m{i}"} for i in range(session.HISTORY_MAX)]
    await session.replace_history("c1", msgs)
    seen = {}

    async def _chat(messages, **kw):
        seen["prompt"] = "\n".join(m["content"] for m in messages)
        return ChatResult(content="合并后的摘要", total_tokens=5)

    monkeypatch.setattr(compact.client, "chat", _chat)
    await compact.maybe_compact("c1")
    assert "此前：用户在排查 401" in seen["prompt"]  # 旧摘要必须参与合并


async def test_摘要为空时不覆盖(fake_redis, monkeypatch):
    await session.set_summary("c1", "原摘要")
    msgs = [{"role": "user", "content": f"m{i}"} for i in range(session.HISTORY_MAX)]
    await session.replace_history("c1", msgs)

    async def _chat(messages, **kw):
        return ChatResult(content="   ", total_tokens=1)

    monkeypatch.setattr(compact.client, "chat", _chat)
    await compact.maybe_compact("c1")
    assert await session.get_summary("c1") == "原摘要"
```

- [ ] **Step 2: 运行确认失败**

```bash
cd backend && .venv/bin/python -m pytest tests/test_compact.py -v
```

预期：`ModuleNotFoundError: No module named 'app.memory.compact'`

- [ ] **Step 3: session 增加摘要读写**

`backend/app/memory/session.py` 顶部常量区加：

```python
SUMMARY_KEEP = 10       # 压缩后保留的最近原文条数
SUMMARY_MAX_CHARS = 1000
```

键函数区加：

```python
def _sum_key(conv_id: str) -> str:
    return f"mem:summary:{conv_id}"
```

文件末尾加：

```python
async def get_summary(conv_id: str) -> str:
    r = get_redis()
    raw = await r.get(_sum_key(conv_id))
    return raw if isinstance(raw, str) else (raw.decode() if raw else "")


async def set_summary(conv_id: str, text: str) -> None:
    if not text or not text.strip():
        return
    r = get_redis()
    await r.set(_sum_key(conv_id), text.strip()[:SUMMARY_MAX_CHARS], ex=ENTITY_TTL)
```

`_FakeRedis` 需支持 `get`/`set`——在 `tests/test_memory_store.py` 的 `_FakeRedis` 里补上：

```python
    async def get(self, key):
        return self.kv.get(key)

    async def set(self, key, val, ex=None):
        self.kv[key] = val
```

并在 `__init__` 加 `self.kv: dict[str, str] = {}`。

- [ ] **Step 4: 实现 compact.py**

创建 `backend/app/memory/compact.py`：

```python
"""滚动摘要压缩：历史触达窗口上限时，把最旧的消息压成摘要。

放在独立模块的原因：压缩需要调 LLM，而 session.py 要保持纯 Redis 无外部依赖。
摘要由用户内容派生且会被重新注入 prompt，因此落库前必须脱敏。
"""

from app.config import settings
from app.guardrail import desensitize
from app.llm import client
from app.memory import session

_SYS = (
    "把下面的技术支持对话压缩成一段不超过 200 字的中文摘要，"
    "保留用户身份特征、已确认的事实（错误码、接口、时间、已排除的原因）与未解决的问题，"
    "丢弃寒暄与重复内容。只输出摘要本身。"
)


async def maybe_compact(conv_id: str) -> bool:
    """历史达到窗口上限则压缩，返回是否执行了压缩。"""
    hist = await session.get_history(conv_id)
    if len(hist) < session.HISTORY_MAX:
        return False

    old, keep = hist[: -session.SUMMARY_KEEP], hist[-session.SUMMARY_KEEP :]
    if not old:
        return False

    previous = await session.get_summary(conv_id)
    body = "\n".join(f"{m['role']}: {m['content']}" for m in old)
    user_content = (f"已有摘要：{previous}\n\n" if previous else "") + f"新增对话：\n{body}"

    r = await client.chat(
        [{"role": "system", "content": _SYS}, {"role": "user", "content": user_content}],
        model=settings.llm_model_small,  # 压缩是简单任务，走小模型降本
        temperature=0.2,
    )
    merged = desensitize.desensitize_text((r.content or "").strip())
    if not merged.strip():
        # 模型没给出有效摘要时保留原摘要，不能把已有记忆覆盖成空
        await session.replace_history(conv_id, keep)
        return True

    await session.set_summary(conv_id, merged)
    await session.replace_history(conv_id, keep)
    return True
```

- [ ] **Step 5: 接入 supervisor**

`state.py` 加：

```python
    history_summary: str             # 滚动摘要（超窗历史的压缩结果）
```

`supervisor.py` 导入 `compact`：

```python
from app.memory import compact, session, store as mem_store, user_profile
```

`load_context` 一并取摘要：

```python
        entities, profile, summary = await asyncio.gather(
            session.get_entities(conv),
            user_profile.profile_line(state["tenant_id"], state.get("user_id", "")),
            session.get_summary(conv),
        )
        return {
            "history": history,
            "collected_entities": entities,
            "user_profile": profile,
            "history_summary": summary,
        }
```

摘要注入意图识别与专家环节——在 `intent_node` 里把摘要并入历史头部：

```python
    async def intent_node(state: AgentState) -> dict:
        history = state.get("history")
        summary = state.get("history_summary")
        if summary and history:
            # 摘要作为最早一条上下文参与指代消解
            history = [{"role": "system", "content": f"早前对话摘要：{summary}"}, *history]
```

（后续 `intent_router.classify(state["query"], history)` 沿用这个 `history` 变量。）

`run()` 末尾在写入本轮消息之后触发压缩：

```python
    await session.append_message(conversation_id, "user", query)
    await session.append_message(conversation_id, "assistant", result["answer"])
    # 历史超窗则压缩最旧部分，避免窗口截断直接丢上下文
    await compact.maybe_compact(conversation_id)
```

- [ ] **Step 6: 运行全量测试**

```bash
cd backend && .venv/bin/python -m pytest tests/ -v
```

预期：全部通过（累计 59 项）

- [ ] **Step 7: ruff 检查**

```bash
cd backend && .venv/bin/python -m ruff check tests
```

预期：`All checks passed`。

只检查 `tests/`：`app/` 有 53 个**改动前就存在**的告警（B008/DTZ/F401 等），
清理它们是另一件事，不属于本计划范围。要确认自己没有新增告警，
用 `git stash` 对比改动前后 `ruff check app` 的条数。

- [ ] **Step 8: 提交**

```bash
git add backend/app/memory/ backend/app/agents/ backend/tests/
git commit -m "feat(memory): 滚动摘要压缩

历史触达 20 条窗口上限时，把最旧 10 条用小模型压成摘要存
mem:summary:{conv_id}，保留最近 10 条原文；摘要作为最早上下文
参与意图识别的指代消解。此前超窗直接 ltrim 丢弃，长对话必然失忆。
摘要由用户内容派生且会回灌 prompt，落库前强制脱敏；
模型给空摘要时保留原摘要不覆盖。"
```

---

## Self-Review

**1. Spec coverage**

| 设计文档章节 | 对应 Task |
| --- | --- |
| §2.1 tool_loop 模块 | Task 3 |
| §2.2 熔断 | Task 3（`FAIL_ROUNDS_TO_BREAK`）+ Task 4（置 need_ticket） |
| §2.3 确定性入参校验 | Task 1 |
| §2.4 各专家工具可见范围 | Task 4 |
| §3 签名工具（含 §3.4 安全约束） | Task 2 |
| §4.2(1) MySQL 回填 | Task 5 |
| §4.2(2) 实体落库 | Task 5 |
| §4.2(3) 用户画像 | Task 6 |
| §4.2(4) 滚动摘要 | Task 7 |
| §4.3 记忆安全约束 | Task 6（脱敏、tenant 条件）+ Task 7（摘要脱敏） |
| §5 数据模型 | Task 6（`UserMemory`）+ Task 5（启用既有两列） |
| §6 可观测 | Task 1（`rejected` 日志）+ Task 3（`invocations`）+ Task 4（透出 + trace 标记熔断） |
| §7 测试 | 各 Task 的 Step 1 |

自查时发现 §6 的"`degraded` 在 trace 中显式标记"最初只落到了 `need_ticket`，
链路上看不出熔断，熔断率无法统计。**已就地补进 Task 4**：两个 Result 各加
`degraded: bool = False` 并透传，`specialists_node` 的 `trace.step` 加
`[熔断降级]` 前缀（Task 4 Step 5）。

**2. Placeholder scan** —— 无 TBD/TODO；每个代码步骤都给了完整可粘贴代码。

**3. Type consistency**

- `ToolLoopResult` 字段名 `content` / `invocations` / `tokens` / `degraded` 在 Task 3 定义，
  Task 4 使用一致。
- `issues` 结构 `{code, detail, fix}` 在 Task 2 全程一致。
- `session.replace_history` 在 Task 5 定义，Task 7 复用，签名一致。
- `profile_line` 返回 `str`（空串表示无记录），Task 6 内一致。
- `_fetch_facts` 返回 `list[dict]`，键为 `kind`/`value`/`hits`，测试与实现一致。
- Task 7 复用 Task 5 的 `_FakeRedis`，已在 Task 7 Step 3 明确要求补 `get`/`set` 与 `kv` 初始化。

---

## 执行顺序与依赖

Task 1 → 2 → 3 → 4 为一条链（工具能力），Task 5 → 6 → 7 为另一条（记忆）。
两条链只在 `supervisor.py` 交汇，可分别交付审查。Task 4 依赖 Task 1/2/3；
Task 6 依赖 Task 5（共用 `load_context` 改动）；Task 7 依赖 Task 5（复用 `replace_history`）。
